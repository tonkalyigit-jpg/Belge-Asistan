# --- çekirdek ---
# Sunucu: hem HTTP API hem derlenmiş React arayüzü (api/server.py).
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # PDF yüklemesi multipart geliyor
pyyaml>=6.0
httpx>=0.27
numpy>=1.26   # BM25 dahil arama tamamen numpy+saf python (belge/store.py)

# --- PDF ---
# pypdfium2: sayfa metni ve sayfa görüntüsü. Sistem bağımlılığı yok (poppler
# gerekmez) ve lisansı izin verici; PyMuPDF AGPL olduğu için tercih edilmedi.
pypdfium2>=4.30
# pdfplumber: kelime koordinatları. Sayfanın kaç sütunlu olduğu ve çizgili
# tabloların nerede olduğu ancak koordinatlardan anlaşılıyor; pypdfium2 metni
# çizim sırasına göre veriyor ve iki sütunlu bir veri sayfasında satırlar iç
# içe geçiyor. Lisansı MIT — PyMuPDF (AGPL) bilerek tercih edilmedi.
pdfplumber>=0.11
pillow>=10.0  # taranmış sayfalarda ön işleme

# --- embedding (lokal, ücretsiz) ---
sentence-transformers>=3.0
torch>=2.2

# --- LLM sağlayıcıları ---
anthropic>=0.40

# --- test ---
pytest>=8.0
